import { AppShell } from "../../components/app/shell"

export const metadata = {
  title: "Long Bread — the app",
  description: "Join a circle, check in, and draw your monthly income.",
}

export default function AppPage() {
  return <AppShell />
}
